VITE_APP_NAME=Deployment Orchestrator (QC)
VITE_BASE_PATH=/deploymentOrchestrator

# The QC build is served by its Spring Boot backend, so API requests remain on
# the same host and port.
VITE_BACKEND_URL=
